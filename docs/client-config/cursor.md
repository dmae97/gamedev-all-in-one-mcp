# Cursor

```json
{
  "mcpServers": {
    "roblox-all-in-one": {
      "command": "node",
      "args": [
        "/absolute/path/to/roblox_all_in_one_mcp/dist/index.js"
      ]
    }
  }
}
```

설정 저장 후 Cursor를 재시작하고 `doctor`를 실행합니다.
